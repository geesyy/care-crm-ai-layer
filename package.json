{
  "name": "care-crm-ai-layer",
  "version": "1.0.0",
  "description": "AI layer (LLM harness, resident intake agent swarm, incident reporting workflow) for a residential care facility CRM",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js",
    "test": "node --test \"tests/**/*.test.js\""
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.104.2",
    "cors": "^2.8.5",
    "dotenv": "^16.4.7",
    "express": "^4.21.2",
    "uuid": "^11.0.3",
    "zod": "^3.24.1",
    "zod-to-json-schema": "^3.24.1"
  }
}
